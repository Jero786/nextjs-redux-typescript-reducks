import reducer from './author.reducers';

import * as authorActions from './author.actions';
import * as authorOperation from './author.operations';
import * as authorSelectors from './author.selectors';

export { authorOperation,  authorActions, authorSelectors };

export default reducer;
